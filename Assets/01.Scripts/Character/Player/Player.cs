
public class Player : Character
{
    
    private MP _mp;
    
    //[SerializeField] private WeaponData;

    private void Start()
    {
        _mp = GetComponent<MP>();
    }

    // Update is called once per frame
    
}
