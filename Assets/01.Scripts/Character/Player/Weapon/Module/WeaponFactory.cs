using UnityEngine;

public static class WeaponFactory
{
    /*
    public static Weapon CreateWeapon(BaseWeaponData data, Transform playerTransform, TargetSystem targetSystem, Transform parent = null)
    {
        Weapon weapon = Object.Instantiate(data.weaponPrefab, parent);
        weapon.Init(data, playerTransform, targetSystem);
       return weapon;
    }
    */
}
